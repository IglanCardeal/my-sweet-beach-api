import { StormGlass } from '@src/clients/stormGlass'

describe('StormGlass Client', () => {
  it('should load StormGlass class from stormGlass.ts', () => {
    expect(StormGlass).toBeDefined()
  })
})
